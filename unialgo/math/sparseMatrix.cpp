#include "unialgo/math/sparseMatrix.hpp"

namespace unialgo {
namespace math {

// All implementations are now in the header using compressed storage
// with if constexpr for layout dispatch.

}  // namespace math
}  // namespace unialgo
